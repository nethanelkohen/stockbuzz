import React from 'react';
import { Link, Route } from 'react-router-dom';

const IndStock = ({ match }) => {
  return (
    <div>
      <Link to={'/'}> Home </Link>
    </div>
  );
};

export default IndStock;
